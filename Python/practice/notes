# This is a single line comment.

```
This is a multiline comment
...
qwsqwdqwdqw
```

A single element tuple is interpreted as string
You can add a trailing comma to tix this
fruits = ('Apples',)

The == operator compares the values of both the operands and checks for value equality.
Whereas is operator checks whether both the operands refer to the same object or not.
x = 20
y = 20
print(x is y)
print(x is not y)
print(x == y)

# Regular expressions
Use / to escape characters
[1-3]   matches number 1 to 3
[^1-3]  NOT number 1 to 3

# Binary search
- searching for a specific value in an ordered collection.
- If the collection is unordered, we can always sort it first before applying Binary Search.

# locals() returns a dictionary that contains all local variables

